<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="project5.team.TeamDao">
	<!-- 팀관리 리스트 -->
	<select id="teamList" resultType="project5.team.TeamVo">
		SELECT m.name name, m.auth auth, m.email email, d.dname dname, d.deptno deptno, p.*
		FROM MEMBER m, DEPARTMENT d, project p
		WHERE m.deptno =d.deptno
	</select>


</mapper>
